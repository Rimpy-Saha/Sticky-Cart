{#
/**
* @file
* Default theme implementation for main view template.
*
* Available variables:
* - attributes: Remaining HTML attributes for the element.
* - css_name: A CSS-safe version of the view name.
* - css_class: The user-specified classes names, if any.
* - header: The optional header.
* - footer: The optional footer.
* - rows: The results of the view query, if any.
* - empty: The content to display if there are no rows.
* - pager: The optional pager next/prev links to display.
* - exposed: Exposed widget form/info to display.
* - feed_icons: Optional feed icons to display.
* - more: An optional link to the next page of results.
* - title: Title of the view, only used when displaying in the admin preview.
* - title_prefix: Additional output populated by modules, intended to be
* displayed in front of the view title.
* - title_suffix: Additional output populated by modules, intended to be
* displayed after the view title.
* - attachment_before: An optional attachment view to be displayed before the
* view content.
* - attachment_after: An optional attachment view to be displayed after the
* view content.
* - dom_id: Unique id for every view being printed to give unique class for
* JavaScript.
*
* @see template_preprocess_views_view()
*
* @ingroup themeable
*/
#}
{%
set classes = [
dom_id ? 'js-view-dom-id-' ~ dom_id,
]
%}
<div{{attributes.addClass(classes)}}>
  {{ title_prefix }}
  {{ title }}
  {{ title_suffix }}

  {% if header %}
  <header>
    {{ header }}
  </header>
  {% endif %}

  {{ exposed }}
  {{ attachment_before }}

  {% if rows -%}
  <div class="view-content">
    {% for item in rows.0['#rows'] %}
    <div id="block-jango-views-block-job-block-1" class="views-row">
      {{ item }}
    </div>
    {% endfor %}
  </div>
  {% elseif empty -%}
  {{ empty }}
  {% endif %}
  {{ pager }}

  {{ attachment_after }}
  {{ more }}

  {% if footer %}
  <footer>
    {{ footer }}
  </footer>s
  {% endif %}

  {{ feed_icons }}
  </div>


  <style>
    #block-jango-views-block-job-block-1.views-row {
      display: grid;
      grid-template-rows: max-content max-content;
      grid-template-columns: 20% 80%;
    }


    #block-jango-views-block-job-block-1 .views-field-title {
      margin-bottom: 20px;
    }

    #block-jango-views-block-job-block-1 .views-field-title span a {
      font-size: 30px;
      font-weight: bold;
    }

    #block-jango-views-block-job-block-1 {
      width: 100%;
      margin: auto;
      box-shadow: 0 4px 40px 0 rgba(0, 0, 0, 0.09), 0 6px 20px 0 rgba(0, 0, 0, 0.17);
      border-radius: 12px;
      transition: 0.5s;
      background-color: white;
      padding: 30px;
      margin: 40px 0;
    }

    #block-jango-views-block-job-block-1 .views-field-field-department>.field-content,
    #block-jango-views-block-job-block-1 .views-field-field-job-city>.field-content {
      font-size: 18px;
    }

    #block-jango-views-block-job-block-1 .views-field-field-department {
      grid-column: 1/2;
    }

    #block-jango-views-block-job-block-1 .views-field-field-about-position,
    #block-jango-views-block-job-block-1 .views-field-title {
      grid-column: span 2;
    }

    #block-jango-views-block-job-block-1 .btn-primary {
      opacity: 1;
      background-color: #004897;
      border-color: #004897;
      color: white
    }

    #block-jango-views-block-job-block-1 .btn-primary:hover {
      opacity: 1;
      background-color: #3276b1;
      border-color: #285e8e;
      color: white;
    }

    #block-jango-views-block-job-block-1 .btn-primary>a {
      color: white;
    }


    @media only screen and (max-width:800px) {
      #block-jango-views-block-job-block-1 .views-row {
        grid-template-columns: 50% 50%;
      }
    }
  </style>